/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['via.placeholder.com'], // 👈 esto habilita imágenes externas de ese dominio
  },
};

module.exports = nextConfig;
